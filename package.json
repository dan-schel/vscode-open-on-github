{
  "name": "open-in-github",
  "displayName": "Open In GitHub",
  "description": "Adds a shortcut to open the current file in GitHub",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.100.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "extensionDependencies": [
    "vscode.git"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "open-in-github.copyGitHubPermalink",
        "title": "Copy GitHub Permalink"
      },
      {
        "command": "open-in-github.openInGitHub",
        "title": "Open in GitHub"
      },
      {
        "command": "open-in-github.openInGitHubMaster",
        "title": "Open in GitHub (on master/main)"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "open-in-github.copyGitHubPermalink",
          "group": "0_openingithub"
        },
        {
          "command": "open-in-github.openInGitHub",
          "group": "0_openingithub"
        },
        {
          "command": "open-in-github.openInGitHubMaster",
          "group": "0_openingithub"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.100.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "eslint": "^9.25.1",
    "typescript": "^5.8.3",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.5.2"
  }
}
